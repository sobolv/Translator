start
    int num = 1;
    int i;
    num = -num;
    print(i);
    int l = scan();
    for (i=0; a <= 10; i+1)
        a = num;

    if a < 12 then
        num = -15;
    fi
    if a < 12 then
        num = -15;
    fi
    n = -h;
     result = n * 5^2^3 + (a^2 - 10 - k) / 10;
end